# apps/web

Next.js + Tailwind frontend. Event feed, event detail, marketplace, chat UI, profile/recommendations.

Scaffold this with:
```
npx create-next-app@latest . --typescript --tailwind --eslint --app
```
Then wire up Supabase client (`@supabase/supabase-js`) using the keys in `.env.local`.
